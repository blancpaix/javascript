노드 는 런타임이고 서버가 아님

프론트나 브라우저는 믿을것이 못됨
그래서 백에서 철저하게 검사를 해줘야 함
서버쪽에서 꼼꼼하게 검사를 해주는게 좋은데 그게 내가 서버를 직접 관리를 한다면 귀찮겟네

미들웨어, 라우터 순서가 엄청 중요함

미들웨어 라우터에서 와일드카드는 라우터 제일 아래 부분으로 내려줘야 함
아니면 위에서 아무거나 걸려서 404 에러 잘뜸 위에서 걸려서 이상하게 처리됨

swagger   api 문서 자동화 추출
  써보세요

DB 연동은.. 그냥 쿼리문 적는거면 하면 될듯?

sequelize
  npx sequelize init 으로 설정, config 폴더 생김 거기서 설정해주면 됨
  model에서 설정
  npx seqeuelize db:create   스키마 생성
  데이터는 json 으로 받음 정확히는 자바스크립트 객체 (함수도 받음,, prototype 등등 toJSON 등등변환해줘야 함)

  예전 $lt 방식 SQL Injection 때문에 사라짐
  const { Op } = require('sequelize'); 로 씀

nodemon -D

passport
  로그인 관리인데 이거 구글, 깃, 카카오, 네이버 다 관리를 해준다고???
  passport-local  이메일 아이디 로그인

dotenv
  .env 파일만 조심해서 관리를 하라는데... 소스코드 상에서는 실제값이 안들어가고 핵심 관리자 끼리만들고잇고 프로젝트 실행할때만잠깐 넣어서 실행하고 나머지 빼고  

morgan
  서버쪽에 요청 응답 기록 남기는 방법

multer
  multipart/form-data (이미지 업로드 시 사용)
  형식의 데이터 폼을 처리하기 위해서 멀터 필요

fs
  파일 시스템을 조작할 수 있는 모듈,
  폴더 없으면 생성하고 기타 등등 가능함

브라우저랑  백이랑 cors 문제 생기는데
cors 모듈로 access control origin 추가해서 해결을 햇는데
도메인이 다르면! 쿠키도 전달이 안됨! 요청을 누가 보냈는지 알 방법이 없음!
proxy, cors module 사용을 하더가
쿠키를 다른 도메인간 전달하고 싶다면
app.use(cors({ credentials: true})); 이거 추가해주면 됨